const StorageFieldNames = Object.freeze({
    Service: "service"
});

const StorageTexts = Object.freeze({
    VersionRequired: "version is required.",
    VersionSemverInvalid: "version must follow semver format like v1.0.0"
});

module.exports = {
    StorageFieldNames,
    StorageTexts
};
